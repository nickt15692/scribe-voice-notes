# server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# pick the backends you actually want; none are required by the server itself
mlx-whisper>=0.4       # Whisper on Apple Silicon
# parakeet-mlx>=0.3    # NVIDIA Parakeet, English only, faster
